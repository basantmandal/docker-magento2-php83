name: Build & Push Multi-Arch Docker Image

on:
  push:
    branches: ["main"]
    tags: ["[0-9]*"]

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Setup QEMU (REQUIRED for ARM builds)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # 3. Setup Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 4. Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 5. Auto metadata (tags + labels)
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: basantmandal/hk2-php8.3-fpm
          tags: |
            type=ref,event=tag
            type=raw,value=latest,enable={{is_default_branch}}

      # 6. Build + Push (MULTI-ARCH)
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        continue-on-error: false
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=php83
          cache-to: ${{ github.ref == 'refs/heads/main' && '' || 'type=gha,mode=min,scope=php83' }}
